# To know more about basic built-in functions in python read the readme file.
# TIP: Use uppercase letters for constant variable names to indicate they shouldn't change.

A = 10      # Constant integer one
B = 20      # Constant integer two 


summation = A + B 
difference = A - B
product = A * B 
average = summation / 2 
distance = abs(difference) 
max_val = max(A, B)
min_val = min(A, B) 


print(f"A. The Sum: {summation}") 
print(f"B. The difference: {difference}") 
print(f"C. The product: {product}") 
print(f"D. The average value: {average}") 
print(f"E. The distance: {distance}") 
print(f"F. The maximum value: {max_val}") 
print(f"G. The minimum value: {min_val}") 








